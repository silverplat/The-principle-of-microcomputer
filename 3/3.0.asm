STACK SEGMENT STACK
	DW 32 DUP(?)
STACK ENDS

CODE SEGMENT
	ASSUME CS:CODE
START:
	MOV AL,10000000B
	MOV DX,0646H
	OUT DX,AL
	MOV DX,0642H
	MOV AL,0FFH
	OUT DX,AL
	
	PUSH DS
	MOV AX,0H
	MOV DS,AX
	
	MOV AX,OFFSET MIR6	;中断入口地址
	MOV SI,0038H		
	MOV [SI],AX
	MOV AX,CS
	MOV SI,003AH 
	MOV [SI],AX
	
	MOV AX,OFFSET MIR7
	MOV SI,003CH
	MOV [SI],AX
	MOV AX,CS
	MOV SI,003EH
	MOV [SI],AX
	
	CLI
	POP DS
	
	MOV AL,11H			;ICW1
	OUT 20H,AL
	MOV AL,08H			;ICW2
	OUT 21H,AL
	MOV AL,04H			;ICW3
	OUT 21H,AL
	MOV AL,03H			;ICW4
	OUT 21H,AL
	MOV AL,00101111B	;OCW1
	OUT 21H,AL

	STI
AA1:
	MOV DX,0642H
	MOV AL,0FFH
	OUT DX,AL
	JMP AA1
MIR7:
	
	MOV DX,0642H
	MOV AL,00001111B
	OUT DX,AL
	
	MOV CX,0FFFFH
	LOOP $
	MOV CX,0FFFFH	
	LOOP $	

	IRET
MIR6:
	
	MOV DX,0642H
	MOV AL,11110000B
	OUT DX,AL

	MOV CX,0FFFFH	
	LOOP $
	MOV CX,0FFFFH
	LOOP $
	
	IRET
	
CODE ENDS
	END START